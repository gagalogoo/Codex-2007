# 兼容性

## 已验证版本

| Codex Windows 版本 | 状态 | 说明 |
| --- | --- | --- |
| `26.715.4045.0` | 通过 | 初始真实 DOM、正常页和设置页基线 |
| `26.715.7063.0` | 通过 | 适配 `#root` 前置 overlay、动态 shell 标记、`role="link"` 返回入口和新版设置菜单 |
| `26.715.8383.0` | 通过 | 新建任务、原生窗控、任务标题、复古输入控件、上下文用量、滚动条与好友栏动画双轮实机验收 |
| `26.903.9818.0` | 通过 | 对话三点悬停菜单、小蓝 QQ 秀包边与形象同款 idle 晃动实机验收 |

验证环境为 Windows、1280 × 820 CSS 视口、DPR 1.5。其他分辨率有响应式规则，但不等同于完整矩阵覆盖。

## 兼容策略

- 外壳：通过左栏与主区共同祖先定位，不依赖 `#root > div:first-child`。
- 标题栏：运行时标记真实 topbar host，CSS 只作用于该宿主。
- 交互：个人资料、模型和发送按钮通过语义属性及可操作性检查识别。
- 设置：兼容 `role="button"` 和 `role="link"` 的“返回应用”；以当前服务行的真实图标为准。
- 失败保护：无法确认原生应用完整时 `verify` 返回非零退出码。

## 不保证稳定的部分

Codex DOM、类名和 Electron/Chromium 行为没有作为第三方主题 API 对外承诺。官方更新可能导致：

- 容器层级、Tailwind 类或语义标签改变；
- 设置菜单增加、重命名或移除；
- 模型/发送按钮在运行和空闲状态间更换；
- CDP 行为随内置 Chromium 变化。

Chrome DevTools Protocol 官方文档也说明 tip-of-tree 协议会频繁变化且不保证向后兼容。升级后应重新运行完整验收，而不是沿用旧 `verify.json`。

## 报告兼容问题

请提供 Codex 版本、视口、失败字段和最小复现。按 [SUPPORT.md](../SUPPORT.md) 脱敏，不要上传完整 DOM、运行截图或会话数据。
